#include "main.h"
/**
 * is_fun_digit - see if is it digit or not
 * @x:our char.
 * Return:one or zero
 * developed by soria
 */

int is_fun_digit(char x)
{
	if (x >= '0' && x <= '9')
		return (1);

	return (0);
}
